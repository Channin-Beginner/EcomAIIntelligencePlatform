---
description: Per-window Conversation.md archive — create on session start, compress after each turn
alwaysApply: true
---

# Conversation 压缩归档（项目）

与全局规则一致。本项目归档路径：

- `.cursor/conversations/<session_id>/Conversation.md`
- `.cursor/sessions/current.json`

每一轮对话结束前，Agent 必须将本轮压缩摘要追加到当前会话的 `Conversation.md`。

恢复上下文时只加载该压缩文件，不要依赖完整 chat transcript。
