---
description: Skills 三层加载 — SKILLS_INDEX → INTRO.md → SKILL.md
alwaysApply: true
---

# Skills 三层分层加载（项目）

与 `~/.cursor/.cursor.md` 规则 2 一致：

1. **第一层**：只读 `~/.cursor/SKILLS_INDEX.md`
2. **第二层**：匹配后最多读 1–2 个 `INTRO.md`
3. **第三层**：确认后只读 1 个 `SKILL.md`

新窗口禁止用 `available_skills` 长列表代替第一层索引。
